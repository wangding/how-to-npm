npm 可以帮助你构建项目，但为了使 npm 做到这一点，你需要告诉 npm 关于项目的一点信息。
你可以在一个名为 package.json 的文件中，告诉 npm 你的项目信息。

运行 “npm init --scope=<username>”，并且将<username>替换成你在上一节课创建的用户名。
这将创建一个 package.json 文件。（额外的挑战，将此项目设置为Git仓库。）

当你完成上述工作后，运行 “$ADVENTURE_COMMAND verify” 