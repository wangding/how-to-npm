当然，在执行 `npm install` 更新之前，显式地检查旧软件包的方法也会很有用。

然而，如果你想懒惰一点，恰好有为此准备的npm命令。 
该命令会将所有相关软件包，提升到 package.json 文件中可接受的最新版本。

你能猜到那是什么命令吗？（`npm help` 可以帮助你）

更新所有相关的软件包到最新版本，然后运行 `$ADVENTURE_COMMAND verify` 来显示有趣的绿色横幅。