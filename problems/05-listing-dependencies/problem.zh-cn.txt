npm 不仅仅使用来安装的。它还能显示你已经安装的软件包（你的依赖项）。

你可以使用 “npm ls” 命令来实现。

在你的工作目录中运行该命令。如果出现任何问题，npm 将返回一条以 “!ERR” 开头的错误信息。

如果你发现一切都OK，则运行 “$ADVENTURE_COMMAND verify OK”，如果遇到任何问题，则运行 “$ADVENTURE_COMMAND verify NOT OK”。