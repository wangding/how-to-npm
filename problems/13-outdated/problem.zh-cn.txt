现在我们已经有了一些依赖项，并且你也学习了可以反复更新自己的软件包，这里存在一个问题：
如果有人更新了自己的依赖包，那我们该怎么办？

第一步是要检测到这一点。 因为我们用版本范围来定义我们的依赖关系，每一次发布都是名称和版本号的唯一组合，
我们使用 `npm outdated` 命令，以可编程的方式来检测兼容的版本。

要想打通此关，请运行 `$ADVENTURE_COMMAND verify PKG`，并将 `PKG` 替换成已经过时的包的名称。
