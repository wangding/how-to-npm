既然你能加入一些东西，那么自然你也会需要将它们删除掉。

输入 `npm rm` 命令（或者 `npm uninstall`，如果你喜欢使用长命令）

删除所有的依赖包！但要确保你不再需要它们。

就像你可以使用 `--save` 来安装软件包，你同样也可使用 `--save` 从package.json文件中删除对软件包的描述。

当删除了你所有的依赖项后，输入 `$ADVENTURE_COMMAND verify` 继续下一关。